---
phase: 06-equip-unequip-flow
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/world.gd
  - scenes/player.tscn
autonomous: false
requirements: [EQUIP-01, EQUIP-02, EQUIP-03, EQUIP-04]

must_haves:
  truths:
    - "InventoryUI has a non-null _equipment_data reference at runtime"
    - "Right-clicking a weapon in the bag and selecting Equip removes it from the bag grid"
    - "The equipped weapon reappears in the bag when a second weapon is equipped"
    - "Closing the inventory and reopening shows correct bag state after equip"
  artifacts:
    - path: "scripts/world.gd"
      provides: "set_equipment_data() wiring call in _ready()"
      contains: "set_equipment_data"
    - path: "scenes/player.tscn"
      provides: "equipment_data Inspector field assigned to player_equipment_data.tres"
      contains: "player_equipment_data"
  key_links:
    - from: "scripts/world.gd:_ready()"
      to: "scripts/inventory_ui.gd:set_equipment_data()"
      via: "inventory_ui.set_equipment_data(player.equipment_data)"
      pattern: "set_equipment_data"
    - from: "scenes/player.tscn"
      to: "scripts/resources/player_equipment_data.tres"
      via: "Inspector equipment_data field"
      pattern: "player_equipment_data"
---

<objective>
Wire `InventoryUI` to `EquipmentData` in `world.gd`, assign `player_equipment_data.tres` to the Player scene, and verify the full equip flow works in-game.

Purpose: Connect the transaction logic implemented in Plan 01 to the live scene graph so the MENU_EQUIP context menu path is fully functional at runtime.
Output: `world.gd` calls `inventory_ui.set_equipment_data(player.equipment_data)` in `_ready()`; Player scene has `equipment_data` Inspector field assigned to `player_equipment_data.tres`; human confirms equip works visually.
</objective>

<execution_context>
@/Users/nicce/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicce/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-equip-unequip-flow/06-01-SUMMARY.md

<interfaces>
<!-- Key contracts for wiring. Extracted from codebase. -->

From scripts/world.gd (current state — add one line):
```gdscript
func _ready() -> void:
    inventory_ui.set_inventory(player.inventory)
    inventory_ui.set_player(player)
    # ADD THIS LINE:
    # inventory_ui.set_equipment_data(player.equipment_data)
    player.inventory.insert_rejected.connect(_on_insert_rejected)
    ...
```

From scripts/inventory_ui.gd (added in Plan 01):
```gdscript
func set_equipment_data(ed: EquipmentData) -> void:
    _equipment_data = ed
```

From scripts/player.gd (added in Phase 5):
```gdscript
@export var equipment_data: EquipmentData
```

player_equipment_data.tres path: res://scripts/resources/player_equipment_data.tres
(Created in Plan 01 — assign this to Player.equipment_data in the Inspector)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire set_equipment_data in world.gd and assign Player Inspector field</name>
  <files>scripts/world.gd, scenes/player.tscn</files>
  <action>
    **Part A — scripts/world.gd:**
    Add one line to `_ready()` after `inventory_ui.set_player(player)`:
    ```gdscript
    inventory_ui.set_equipment_data(player.equipment_data)
    ```
    This is the only change to `world.gd`. Do not restructure or reorder existing lines.

    **Part B — scenes/player.tscn:**
    Assign `player_equipment_data.tres` to the `equipment_data` Inspector field on the Player root node. In `scenes/player.tscn`, find the `[node name="Player" ...]` section (the root node) and add or update the `equipment_data` property to reference `scripts/resources/player_equipment_data.tres`.

    To do this in the `.tscn` file directly:
    1. Add an `ext_resource` entry for `player_equipment_data.tres` near the top of the file:
       ```
       [ext_resource type="Resource" uid="..." path="res://scripts/resources/player_equipment_data.tres" id="N_equip"]
       ```
       Use the uid from `player_equipment_data.tres`. Use a unique resource ID (e.g. `"equip_data"`).
    2. In the `[node name="Player" ...]` block, add:
       ```
       equipment_data = ExtResource("equip_data")
       ```

    After editing both files, run `make lint && make format-check && make test` to confirm no regressions.
  </action>
  <verify>
    <automated>make lint &amp;&amp; make format-check &amp;&amp; make test</automated>
  </verify>
  <done>
    `scripts/world.gd` contains `inventory_ui.set_equipment_data(player.equipment_data)`.
    `scenes/player.tscn` references `player_equipment_data.tres` for the `equipment_data` field.
    `make lint`, `make format-check`, `make test` all pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — full equip flow in-game</name>
  <files>none</files>
  <action>
    Human opens Godot, presses F5, and verifies the equip flow works as described in how-to-verify. No code changes — this task pauses for user confirmation.
  </action>
  <verify>Human confirms equip flow works in-game per the steps below.</verify>
  <done>Human types "approved" after confirming EQUIP-01 and EQUIP-02 visual behaviour.</done>
  <what-built>
    Full equip/unequip flow: atomic transaction logic (Plan 01) + runtime wiring (Task 1). The MENU_EQUIP context menu path is now live.
  </what-built>
  <how-to-verify>
    Open the project in Godot and press F5 to run.

    **Test 1 — EQUIP-01: Equip weapon from bag**
    1. Pick up a weapon item (e.g. sword) so it appears in the inventory bag.
    2. Open inventory (Tab or I).
    3. Right-click the weapon slot in the bag.
    4. Select "Equip" from the context menu.
    5. Expected: The weapon disappears from the bag grid. No crash. No duplicate item visible.

    **Test 2 — EQUIP-02: Swap weapon**
    1. With a weapon already equipped (from Test 1), pick up a second weapon.
    2. Right-click the second weapon in the bag and select "Equip".
    3. Expected: The second weapon is now in the equipment slot (not visible in bag). The first weapon reappears in the bag at an empty slot.

    **Test 3 — EQUIP-03: Unequip rejected when bag full**
    (This tests the data path; full UI for unequip is Phase 8. If there is no unequip UI trigger yet, skip and note.)
    If a temporary unequip trigger exists: Fill the bag completely, then trigger unequip.
    Expected: Weapon stays equipped; bag unchanged; rejection label may appear.

    **Test 4 — EQUIP-04: Equip tool (if a tool item exists in game)**
    1. If a tool-category item is available, pick it up.
    2. Right-click it in the bag; confirm "Equip" appears in the context menu.
    3. Select "Equip".
    4. Expected: Tool disappears from bag; no crash.

    Report any visual glitches, crashes, or incorrect behaviour.
  </how-to-verify>
  <resume-signal>Type "approved" if the equip flow works as described, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
After Task 1 and checkpoint approval:
- `make lint && make format-check && make test` passes
- `scripts/world.gd` has the `set_equipment_data` wiring call
- `scenes/player.tscn` has the equipment_data resource assigned
- Human confirmed equip/unequip flow works in-game (EQUIP-01, EQUIP-02 at minimum)
</verification>

<success_criteria>
- Runtime equip flow works: weapon moves from bag to equipment slot on Equip menu selection
- Swap flow works: old weapon returns to bag when new weapon is equipped
- No null-reference crash on equip (equipment_data is properly wired)
- All automated checks pass: lint, format-check, tests
- Human verification approved for EQUIP-01 and EQUIP-02 visual behaviour
</success_criteria>

<output>
After completion, create `.planning/phases/06-equip-unequip-flow/06-02-SUMMARY.md`
</output>
