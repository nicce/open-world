---
phase: 09-foundation
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [scripts/resources/inventory.gd, scripts/resources/inventory_slot.gd]
autonomous: true
requirements: [SER-01, SER-02]
must_haves:
  truths:
    - Inventory `to_dict()` returns a sparse dictionary with `"id"` and `"qty"` keys.
    - Inventory `from_dict()` restores full state and emits `inventory_changed`.
  artifacts:
    - path: "scripts/resources/inventory.gd"
      provides: "Inventory serialisation logic"
    - path: "scripts/resources/inventory_slot.gd"
      provides: "InventorySlot serialisation logic"
  key_links:
    - from: "scripts/resources/inventory.gd"
      to: "scripts/item_registry.gd"
      via: "ItemRegistry.get_item()"
      pattern: "ItemRegistry\\.get_item"
---

<objective>
Enable serialisation for inventory data via to_dict() and from_dict() methods.

Purpose: Allow inventory contents to be saved as JSON-friendly dictionaries and restored on game start.
Output: Serialisation logic in Inventory and InventorySlot.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.2-phases/09-foundation-itemregistry-and-resource-serialisation/09-CONTEXT.md
@.planning/milestones/v1.2-phases/09-foundation-itemregistry-and-resource-serialisation/RESEARCH.md
@scripts/resources/inventory.gd
@scripts/resources/inventory_slot.gd
@scripts/item_registry.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement serialisation in InventorySlot</name>
  <files>scripts/resources/inventory_slot.gd</files>
  <action>
    Add to_dict() and from_dict() to InventorySlot.
    - `to_dict()`: returns `{"id": item.id if item else "", "qty": quantity}` (D-05 shape).
    - `from_dict(dict)`: sets item using `ItemRegistry.get_item(dict["id"])`, sets `quantity`.
  </action>
  <verify>
    <automated>make lint</automated>
  </verify>
  <done>InventorySlot supports dict round-trip.</done>
</task>

<task type="auto">
  <name>Task 2: Implement serialisation in Inventory</name>
  <files>scripts/resources/inventory.gd</files>
  <action>
    Add to_dict() and from_dict() to Inventory.
    - `to_dict()`: returns a sparse dictionary (D-05) keyed by slot index string. Shape: `{"slots": {"2": {"id": "sword", "qty": 1}, ...}}`. Only serialise occupied slots. Sparse keys for indices (per D-05).
    - `from_dict(dict)`: Clear all slots first (D-06), then assign item/quantity from the dictionary. Mutate in place (SER-02). Emit `inventory_changed` once after restoration.
    
    Ensure string keys for dictionary indices (Pitfall 1 from RESEARCH.md).
  </action>
  <verify>
    <automated>make lint</automated>
  </verify>
  <done>Inventory supports sparse dict round-trip, mutating in place.</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for Inventory serialisation</name>
  <files>tests/unit/test_inventory_serialisation.gd</files>
  <action>
    Create unit tests for Inventory round-trip.
    - Test that an inventory with items serialises to a sparse dict.
    - Test that `from_dict()` on a DIFFERENT inventory instance correctly restores items and quantities.
    - Test that `from_dict()` clears existing items before restoration (D-06).
  </action>
  <verify>
    <automated>make test tests/unit/test_inventory_serialisation.gd</automated>
  </verify>
  <done>Inventory round-trip is verified stable.</done>
</task>

</tasks>

<verification>
Check that Inventory.to_dict() / from_dict() round-trip preserves all slot item ids and quantities.
</verification>

<success_criteria>
1. Inventory.to_dict() / from_dict() round-trip preserves all slot item ids and quantities (SER-01).
2. from_dict() clears all slots before restoration (D-06).
3. from_dict() mutates Resource instances in place (SER-02).
</success_criteria>

<output>
After completion, create `.planning/phases/09-foundation/09-02-SUMMARY.md`
</output>
