---
phase: 09-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/item_registry.gd, project.godot, resources/items/sword.tres, resources/items/axe.tres, resources/items/medpack.tres, resources/items/gold_key.tres]
autonomous: true
requirements: [REG-01, REG-02]
must_haves:
  truths:
    - ItemRegistry resolves `&"sword"` to the correct Resource instance.
    - ItemRegistry logs a warning and returns `null` for unknown IDs.
  artifacts:
    - path: "scripts/item_registry.gd"
      provides: "ID -> Resource resolution"
    - path: "resources/items/sword.tres"
      provides: "Standalone sword resource"
  key_links: []
---

<objective>
Create standalone item resources and a registry to resolve item IDs for serialisation.

Purpose: Decouple item data from scene instances so inventory slots can be serialised as simple IDs.
Output: Standalone .tres files and an ItemRegistry autoload.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.2-phases/09-foundation-itemregistry-and-resource-serialisation/09-CONTEXT.md
@.planning/milestones/v1.2-phases/09-foundation-itemregistry-and-resource-serialisation/RESEARCH.md
@scripts/resources/item.gd
@scripts/resources/weapon_item.gd
@scripts/resources/health_item.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create standalone Item Resources</name>
  <files>resources/items/sword.tres, resources/items/axe.tres, resources/items/medpack.tres, resources/items/gold_key.tres</files>
  <action>
    Create standalone .tres files for existing items in `res://resources/items/` (per RESEARCH.md).
    - sword.tres: WeaponItem, id=&"sword", name="Sword", damage=3.0
    - axe.tres: WeaponItem, id=&"axe", name="Axe", damage=2.0, texture=res://art/items/axe.png
    - medpack.tres: HealthItem, id=&"medpack", name="Medpack", health=50, texture=res://art/potions/Medipack.png
    - gold_key.tres: Item, id=&"gold_key", name="Gold Key", texture=res://art/items/GoldKey.png
    
    Ensure StringName IDs are used (DATA-01). Reference existing sub_resources in scenes/sword.tscn, scenes/axe.tscn, and scenes/gold_key.tscn for values.
  </action>
  <verify>
    <automated>ls resources/items/*.tres | wc -l # Should be 4</automated>
  </verify>
  <done>Four .tres files exist in res://resources/items/ with correct IDs and properties.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ItemRegistry autoload</name>
  <files>scripts/item_registry.gd, project.godot</files>
  <action>
    Implement ItemRegistry as a script-only autoload (D-01).
    - Create `scripts/item_registry.gd`.
    - Implement `_build_registry()`: private function populating a dictionary with hardcoded `preload()` calls for each item (D-02).
    - Implement `get_item(id: StringName) -> Item`: public interface for resolution (D-03).
    - Handle unknown IDs: `push_warning()` and return `null` (D-04, REG-02).
    - Register "ItemRegistry" in `project.godot`.
  </action>
  <verify>
    <automated>grep "ItemRegistry" project.godot && make lint</automated>
  </verify>
  <done>ItemRegistry is registered and can resolve registered IDs via get_item().</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for ItemRegistry</name>
  <files>tests/unit/test_item_registry.gd</files>
  <action>
    Create unit tests for ItemRegistry using GUT.
    - Test that `get_item(&"sword")` returns a WeaponItem with damage 3.0.
    - Test that `get_item(&"medpack")` returns a HealthItem with health 50.
    - Test that `get_item(&"unknown")` returns null and logs a warning.
  </action>
  <verify>
    <automated>make test tests/unit/test_item_registry.gd</automated>
  </verify>
  <done>ItemRegistry tests pass with 100% coverage of core logic.</done>
</task>

</tasks>

<verification>
Check that all item .tres files are valid and ItemRegistry can resolve them headlessly.
</verification>

<success_criteria>
1. ItemRegistry autoload resolves any registered item id to its typed Resource instance (REG-01).
2. ItemRegistry returns null and logs a warning for any unknown id (REG-02).
</success_criteria>

<output>
After completion, create `.planning/phases/09-foundation/09-01-SUMMARY.md`
</output>
