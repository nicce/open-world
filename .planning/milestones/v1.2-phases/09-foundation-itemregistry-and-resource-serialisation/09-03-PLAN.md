---
phase: 09-foundation
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified: [scripts/resources/equipment_data.gd, scripts/health_component.gd]
autonomous: true
requirements: [SER-03, SER-04, SER-05]
must_haves:
  truths:
    - EquipmentData `to_dict()` returns `"weapon"` and `"tool"` keys.
    - EquipmentData `from_dict()` restores state using `equip_*` methods.
    - HealthComponent `load_health()` updates health and the health bar without full re-initialisation.
  artifacts:
    - path: "scripts/resources/equipment_data.gd"
      provides: "Equipment serialisation logic"
    - path: "scripts/health_component.gd"
      provides: "Health restoration logic"
  key_links:
    - from: "scripts/resources/equipment_data.gd"
      to: "scripts/item_registry.gd"
      via: "ItemRegistry.get_item()"
      pattern: "ItemRegistry\\.get_item"
    - from: "scripts/health_component.gd"
      to: "scripts/health_bar.gd"
      via: "health_bar.update()"
      pattern: "health_bar\\.update"
---

<objective>
Enable serialisation for equipment and health data via serialisation methods.

Purpose: Allow equipment and player health to be saved and restored from disk.
Output: to_dict/from_dict logic in EquipmentData and load_health logic in HealthComponent.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/resources/equipment_data.gd
@scripts/health_component.gd
@scripts/item_registry.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement serialisation in EquipmentData</name>
  <files>scripts/resources/equipment_data.gd</files>
  <action>
    Add to_dict() and from_dict() to EquipmentData class.
    - to_dict(): returns `{"weapon": weapon.id if weapon else null, "tool": tool.id if tool else null}` (D-07). Use 'null' for unequipped state.
    - from_dict(dict): resolves weapon and tool IDs using ItemRegistry. Calls `equip_weapon` and `equip_tool` on self to fire signals when restoring (SER-04).
    
    Ensure from_dict mutates the instance in place.
  </action>
  <verify>
    <automated>make lint</automated>
  </verify>
  <done>Equipment serialisation is implemented and signal-stable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement load_health in HealthComponent</name>
  <files>scripts/health_component.gd</files>
  <action>
    Implement `load_health(value: int)` in HealthComponent.
    - Sets `health = clampi(value, 0, max_health)`.
    - Updates `health_bar` if it exists (D-10).
    - Does NOT trigger `_ready() re-init` logic (per SER-05).
  </action>
  <verify>
    <automated>make lint</automated>
  </verify>
  <done>Health component can be updated to a specific HP value without re-initialising max_health.</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for Equipment and Health serialisation</name>
  <files>tests/unit/test_equipment_serialisation.gd, tests/unit/test_health_serialisation.gd</files>
  <action>
    Create unit tests for EquipmentData and HealthComponent loading.
    - EquipmentData: Test to_dict/from_dict round-trip with a sword and axe. Verify `equipment_changed` signal is emitted on from_dict().
    - HealthComponent: Test that `load_health(25)` sets health to 25 and updates health_bar (use GUT double for health_bar).
  </action>
  <verify>
    <automated>make test tests/unit/test_equipment_serialisation.gd tests/unit/test_health_serialisation.gd</automated>
  </verify>
  <done>Equipment and Health loading logic is verified stable.</done>
</task>

</tasks>

<verification>
Check that EquipmentData.to_dict() / from_dict() round-trip preserves weapon and tool slots, and HealthComponent.load_health(value) correctly updates HP.
</verification>

<success_criteria>
1. EquipmentData.to_dict() / from_dict() round-trip preserves weapon and tool slot contents (SER-03, SER-04).
2. HealthComponent.load_health(value) sets HP correctly (SER-05).
3. from_dict() mutates Resource instances in place and fires signals.
</success_criteria>

<output>
After completion, create `.planning/phases/09-foundation/09-03-SUMMARY.md`
</output>
