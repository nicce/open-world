---
phase: 10-savemanager-write-path-and-player-round-trip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/save_manager.gd, tests/unit/test_save_manager.gd, project.godot]
autonomous: true
requirements: [SAVE-01]

must_haves:
  truths:
    - "SaveManager exists as a script-only autoload"
    - "SaveManager.save_game writes to user://save.tmp before renaming to user://save.json"
    - "SaveManager.load_game returns the Dictionary saved by save_game"
  artifacts:
    - path: "scripts/save_manager.gd"
      provides: "Atomic write and load logic"
    - path: "tests/unit/test_save_manager.gd"
      provides: "Verification of file I/O safety"
  key_links:
    - from: "scripts/save_manager.gd"
      to: "user://save.json"
      via: "FileAccess"
---

<objective>
Create the SaveManager autoload and implement robust atomic file writing for JSON data.

Purpose: Provide a reliable foundation for game persistence that avoids file corruption.
Output: scripts/save_manager.gd, tests/unit/test_save_manager.gd
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SaveManager autoload</name>
  <files>scripts/save_manager.gd, project.godot</files>
  <action>
    - Create `scripts/save_manager.gd` extending `Node`.
    - Register `SaveManager` in `project.godot` under `[autoload]` as `*res://scripts/save_manager.gd`.
    - Define constants for `SAVE_PATH = "user://save.json"` and `TMP_PATH = "user://save.tmp"`.
  </action>
  <verify>
    <automated>grep "SaveManager" project.godot && ls scripts/save_manager.gd</automated>
  </verify>
  <done>SaveManager is accessible as a global Autoload.</done>
</task>

<task type="auto">
  <name>Task 2: Implement atomic save/load logic</name>
  <files>scripts/save_manager.gd</files>
  <action>
    - Implement `save_data(data: Dictionary) -> Error`:
      - Serialize `data` to JSON string.
      - Open `TMP_PATH` for WRITE.
      - Store string and close.
      - Use `DirAccess.remove_absolute(SAVE_PATH)` if it exists to ensure rename works on all platforms.
      - Use `DirAccess.rename_absolute(TMP_PATH, SAVE_PATH)` to finalize.
      - Return `OK` or error code.
    - Implement `load_data() -> Dictionary`:
      - Return empty dictionary if `SAVE_PATH` doesn't exist.
      - Open `SAVE_PATH` for READ.
      - Read text and parse JSON.
      - Return parsed Dictionary or empty dictionary on failure.
  </action>
  <verify>
    <automated>cat scripts/save_manager.gd | grep -E "save_data|load_data" && make lint</automated>
  </verify>
  <done>Basic Dictionary-based save/load with robust atomic write is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for SaveManager I/O</name>
  <files>tests/unit/test_save_manager.gd</files>
  <behavior>
    - test_save_and_load_roundtrip: Saving a dict should return the same dict on load.
    - test_load_non_existent_returns_empty: Loading when no file exists returns {}.
    - test_atomic_write_uses_tmp: Verify that TMP_PATH is used during save (can mock or check existence during partial operation).
  </behavior>
  <action>
    Implement unit tests in `tests/unit/test_save_manager.gd`. 
    Ensure to clean up test files in `user://` after tests if possible, or use specific test paths.
  </action>
  <verify>
    <automated>./bin/godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gtest=res://tests/unit/test_save_manager.gd</automated>
  </verify>
  <done>SaveManager I/O is verified to be reliable and handle edge cases.</done>
</task>

</tasks>

<verification>
Run all unit tests to ensure no regressions in serialisation logic from Phase 9.
</verification>

<success_criteria>
- SaveManager is a registered Autoload.
- Unit tests for SaveManager pass.
- save.json is only created/updated via atomic rename.
</success_criteria>

<output>
After completion, create `.planning/phases/10-savemanager-write-path-and-player-round-trip/10-01-SUMMARY.md`
</output>
