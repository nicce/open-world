---
phase: 10-savemanager-write-path-and-player-round-trip
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: [scripts/player.gd, scripts/world.gd, scripts/save_manager.gd, scripts/campfire.gd, scenes/campfire_menu.tscn, scripts/campfire_menu.gd, tests/unit/test_player_save_load.gd]
autonomous: true
requirements: [SAVE-02, SAVE-04]

must_haves:
  truths:
    - "Player position and HP are persisted correctly between saves"
    - "Restarting the game automatically restores player state from save.json if it exists"
    - "Interacting with a Campfire opens a menu with a 'Save' option"
    - "Missing save file on start does not cause errors"
  artifacts:
    - path: "scripts/player.gd"
      provides: "to_dict/from_dict serialization"
    - path: "scenes/campfire_menu.tscn"
      provides: "UI for saving/sleeping"
    - path: "scripts/world.gd"
      provides: "Integration point for save/load on start"
  key_links:
    - from: "scripts/campfire.gd"
      to: "scenes/campfire_menu.tscn"
      via: "Instantiating menu on interaction"
---

<objective>
Enable the Player to serialize its state and integrate the SaveManager via a Campfire interaction menu.

Purpose: Allow players to keep their progress (location and health) by interacting with campfires in the world.
Output: Integrated player persistence triggered by world objects.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.2-phases/10-savemanager-write-path-and-player-round-trip/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add serialization to Player</name>
  <files>scripts/player.gd</files>
  <action>
    - Add `to_dict() -> Dictionary` to `Player` class:
      - Include `"position": {"x": position.x, "y": position.y}`.
      - Include `"health": health_component.health`.
    - Add `from_dict(dict: Dictionary) -> void` to `Player` class:
      - Restore `position` using `Vector2(dict.position.x, dict.position.y)`.
      - Restore HP using `health_component.load_health(dict.health)`.
  </action>
  <verify>
    <automated>cat scripts/player.gd | grep -E "to_dict|from_dict"</automated>
  </verify>
  <done>Player can convert its state to/from a JSON-compatible Dictionary.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SaveManager high-level API</name>
  <files>scripts/save_manager.gd</files>
  <action>
    - Implement `save_game(player: Player)`:
      - Create data dictionary with `"player": player.to_dict()`.
      - Call `save_data(data)`.
    - Implement `load_game(player: Player)`:
      - Call `load_data()`.
      - If result has `"player"` key, call `player.from_dict(result["player"])`.
  </action>
  <verify>
    <automated>cat scripts/save_manager.gd | grep -E "save_game|load_game"</automated>
  </verify>
  <done>SaveManager handles Player-specific saving and loading.</done>
</task>

<task type="auto">
  <name>Task 3: Create CampfireMenu UI</name>
  <files>scenes/campfire_menu.tscn, scripts/campfire_menu.gd</files>
  <action>
    - Create a simple centered menu (Popup or Panel).
    - Add "Save" and "Sleep" buttons.
    - "Save" button calls `SaveManager.save_game(player)`.
    - "Sleep" button (Phase 12 placeholder: prints "Sleeping..." and closes).
    - Ensure menu is centered and pauses game if necessary.
  </action>
  <verify>
    <automated>ls scenes/campfire_menu.tscn scripts/campfire_menu.gd</automated>
  </verify>
  <done>Campfire menu UI exists with Save/Sleep options.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Campfire interaction</name>
  <files>scripts/campfire.gd, scripts/world.gd</files>
  <action>
    - Modify `scripts/campfire.gd`:
      - Update `_on_interact_area_body_entered(body)` to store body in `player_ref` if it is a Player.
      - Update `_on_interact_area_body_exited(body)` to clear `player_ref`.
      - Move interaction logic from `_physics_process` to `_process` for consistency with `Collectable.gd`.
      - On "interact", instead of `add_wood(1)`, instantiate `CampfireMenu`.
      - Add the menu as a child of the `CanvasLayer` (find via `get_tree().root.find_child("CanvasLayer", true, false)` or similar reliable path).
      - Pass `player_ref` to the menu instance.
    - Modify `scripts/world.gd`:
      - At the end of `_ready()`, call `SaveManager.load_game(player)`.
  </action>
  <verify>
    <automated>grep "player_ref" scripts/campfire.gd && grep "SaveManager.load_game" scripts/world.gd && make lint</automated>
  </verify>
  <done>Campfire interaction triggers the save menu on the UI layer; game loads on start.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Integration tests for Player Save/Load</name>
  <files>tests/unit/test_player_save_load.gd</files>
  <behavior>
    - test_player_round_trip: 
      1. Create Player, set position/health. 
      2. Call `save_game(player)`.
      3. Create new Player (or reset current). 
      4. Call `load_game(new_player)`. 
      5. Assert position and health match.
  </behavior>
  <action>
    Create and implement `tests/unit/test_player_save_load.gd`.
  </action>
  <verify>
    <automated>./bin/godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gtest=res://tests/unit/test_player_save_load.gd</automated>
  </verify>
  <done>Player persistence is verified through a full round-trip integration test.</done>
</task>

</tasks>

<verification>
Interact with campfire, select 'Save', restart game, and verify player position/HP are restored.
</verification>

<success_criteria>
- Interacting with campfire opens Save/Sleep menu.
- 'Save' button creates valid user://save.json.
- Game loads player data on start.
- HP and Position are restored correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/10-savemanager-write-path-and-player-round-trip/10-02-SUMMARY.md`
</output>
