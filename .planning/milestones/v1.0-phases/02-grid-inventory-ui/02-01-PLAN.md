---
phase: 02-grid-inventory-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/resources/inventory.gd
  - tests/unit/test_inventory.gd
  - tests/unit/test_inventory_ui_helpers.gd
autonomous: true
requirements:
  - INV-01
  - INV-02

must_haves:
  truths:
    - "inventory.gd emits inventory_changed when at least one item is inserted"
    - "inventory.gd emits insert_rejected when weight prevents any insertion"
    - "inventory_changed is NOT emitted when nothing was inserted (full inventory)"
    - "abbreviate() trims names longer than max_len and returns short names unchanged"
    - "Weight format helper produces 'X.X / Y kg' string"
  artifacts:
    - path: "scripts/resources/inventory.gd"
      provides: "inventory_changed and insert_rejected signals; emit rules on insert()"
      contains: "signal inventory_changed"
    - path: "tests/unit/test_inventory_ui_helpers.gd"
      provides: "Unit tests for abbreviate() and weight format"
      exports: []
    - path: "tests/unit/test_inventory.gd"
      provides: "Signal emission tests for insert_rejected and inventory_changed"
      contains: "test_insert_rejected_signal"
  key_links:
    - from: "scripts/resources/inventory.gd"
      to: "signal inventory_changed"
      via: "emitted inside insert() when remaining < amount"
      pattern: "inventory_changed\\.emit"
    - from: "scripts/resources/inventory.gd"
      to: "signal insert_rejected"
      via: "emitted inside insert() when to_add <= 0 (weight blocks all)"
      pattern: "insert_rejected\\.emit"
---

<objective>
Add signals to the Inventory resource and write all failing unit tests (Wave 0 RED) before any UI work begins.

Purpose: The signal-driven UI pattern requires inventory_changed and insert_rejected to exist on the Inventory resource. Tests are written first (RED) then the signals are added (GREEN). This unblocks Plans 02 and 03 which depend on these signals.

Output: Two new test files (or extensions), updated inventory.gd with signals, all tests green.
</objective>

<execution_context>
@/Users/nicce/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicce/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-grid-inventory-ui/02-CONTEXT.md
@.planning/phases/02-grid-inventory-ui/02-RESEARCH.md

@scripts/resources/inventory.gd
@scripts/resources/inventory_slot.gd
@scripts/resources/item.gd
@tests/unit/test_inventory.gd
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for Inventory signals and UI helpers (RED)</name>
  <files>tests/unit/test_inventory.gd, tests/unit/test_inventory_ui_helpers.gd</files>
  <behavior>
    Signal tests (add to test_inventory.gd):
    - test_inventory_changed_emitted_on_successful_insert: insert 1 item into empty inventory → inventory_changed emitted exactly once
    - test_inventory_changed_not_emitted_when_nothing_inserted: fill inventory completely, then try to insert 1 more → inventory_changed NOT emitted
    - test_insert_rejected_emitted_when_weight_blocks_all: inventory at 0 remaining weight (or item too heavy), insert 1 → insert_rejected emitted exactly once
    - test_insert_rejected_not_emitted_on_successful_insert: normal insert → insert_rejected NOT emitted

    Helper tests (new file test_inventory_ui_helpers.gd):
    - test_abbreviate_short_name_unchanged: abbreviate("Axe", 4) == "Axe"
    - test_abbreviate_long_name_trimmed: abbreviate("Medicpack", 4) == "Medi"
    - test_abbreviate_exact_length_unchanged: abbreviate("Wood", 4) == "Wood"
    - test_weight_format_string: format_weight(12.5, 20.0) == "12.5 / 20 kg"
    - test_weight_format_string_zero: format_weight(0.0, 50.0) == "0.0 / 50 kg"

    GUT signal assertion pattern:
    - Use watch_signals(inventory) before the action, then assert_signal_emitted(inventory, "inventory_changed")
    - Use assert_signal_not_emitted(inventory, "inventory_changed") for the negative cases

    The helper tests call static functions that do not exist yet (InventoryUIHelpers.abbreviate, InventoryUIHelpers.format_weight). These tests will FAIL (RED) until Task 2 creates the functions.

    Run make test after writing — confirm tests are RED for the new signal/helper tests, and all pre-existing tests remain GREEN.
  </behavior>
  <action>
    1. Open tests/unit/test_inventory.gd and add the four signal tests at the bottom of the file. Use the existing _make_inventory() and _make_item() helpers. Use watch_signals() / assert_signal_emitted() / assert_signal_not_emitted() from GUT.

    2. Create tests/unit/test_inventory_ui_helpers.gd as a new GutTest. Define static method stubs being called: InventoryUIHelpers.abbreviate(name, max_len) and InventoryUIHelpers.format_weight(current, max_w). These classes do not exist yet — tests will error/fail (RED).

    3. Run: make lint && make format-check && make test
       Expected: Pre-existing tests GREEN. New signal and helper tests RED.

    Note: GUT's watch_signals() requires the signal to be declared on the object. Until inventory.gd has the signals declared, the signal tests will fail with "signal not found". That is the correct RED state.
  </action>
  <verify>
    <automated>make lint && make format-check && make test 2>&1 | tail -30</automated>
  </verify>
  <done>New test functions exist in both test files. Pre-existing suite stays green. New tests for signals and helpers are RED (failing). No lint or format errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add signals to Inventory and implement InventoryUIHelpers (GREEN)</name>
  <files>scripts/resources/inventory.gd, scripts/inventory_ui_helpers.gd</files>
  <behavior>
    inventory.gd changes:
    - Add: signal inventory_changed
    - Add: signal insert_rejected
    - In insert(): after the two loops, if (amount - remaining) > 0, emit inventory_changed. If remaining == amount and weight-blocked (to_add <= 0 on first attempt), emit insert_rejected.
    - Emit logic: track whether any items were inserted. If yes, emit inventory_changed. If no items inserted AND the reason was weight (to_add was 0), emit insert_rejected.
    - Do NOT emit inventory_changed when nothing was inserted (remaining == amount).

    Exact emit placement in insert():
    After both loops complete:
      var inserted = amount - remaining
      if inserted > 0:
          inventory_changed.emit()
      elif remaining > 0:
          # nothing inserted; check if weight-blocked
          var weight_budget = floori(remaining_weight() / item.weight)
          if weight_budget <= 0:
              insert_rejected.emit()

    scripts/inventory_ui_helpers.gd (new file):
    - class_name InventoryUIHelpers (no extends — pure static helpers)
    - static func abbreviate(item_name: String, max_len: int = 4) -> String
      - if item_name.length() <= max_len: return item_name
      - return item_name.left(max_len)
    - static func format_weight(current: float, max_w: float) -> String
      - return "%.1f / %.0f kg" % [current, max_w]

    Note: GDScript does not allow a bare class without extends. Use "extends RefCounted" if the class needs to be instantiable. Since these are purely static, use "extends Object" or just omit extends and keep only static funcs. Actually in GDScript 4, a script with only static funcs can extend nothing — use "class_name InventoryUIHelpers" with no extends line. Verify this compiles under make lint; if lint rejects it, add "extends RefCounted".
  </behavior>
  <action>
    1. Edit scripts/resources/inventory.gd:
       - Add signal declarations at top (after class_name / extends line).
       - Modify insert() to emit signals per the behavior spec above.
       - Do NOT change insert()'s return value or any existing logic.

    2. Create scripts/inventory_ui_helpers.gd with class_name InventoryUIHelpers, abbreviate(), and format_weight() static functions.

    3. Run: make lint && make format-check && make test
       All tests must be GREEN.
  </action>
  <verify>
    <automated>make lint && make format-check && make test</automated>
  </verify>
  <done>make test passes with zero failures. inventory.gd has signal inventory_changed and signal insert_rejected. scripts/inventory_ui_helpers.gd exists with both static functions. All signal and helper tests are GREEN.</done>
</task>

</tasks>

<verification>
make lint && make format-check && make test — all pass with zero failures.
Signal tests for inventory_changed and insert_rejected are green.
Helper tests for abbreviate() and format_weight() are green.
Pre-existing Phase 1 tests unbroken.
</verification>

<success_criteria>
- inventory.gd declares signal inventory_changed and signal insert_rejected
- insert() emits inventory_changed when at least 1 item inserted; emits insert_rejected when weight blocks all insertion
- insert() does NOT emit inventory_changed when nothing was inserted
- InventoryUIHelpers.abbreviate("Medicpack", 4) returns "Medi"
- InventoryUIHelpers.format_weight(12.5, 20.0) returns "12.5 / 20 kg"
- make test passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/02-grid-inventory-ui/02-01-SUMMARY.md`
</output>
