---
phase: 03-item-management
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified: []
autonomous: false
requirements:
  - ITEM-01
  - ITEM-02
  - ITEM-03

must_haves:
  truths:
    - "Player can use a medpack to restore HP (slot quantity decrements, HP visually increases)"
    - "Player can drop a key and it reappears as a collectable near the player"
    - "Player can pick up the dropped key and see a pickup notification label"
    - "All three actions work end-to-end in the running game"
  artifacts:
    - path: "scenes/world.tscn"
      provides: "Full assembled scene with all Phase 3 nodes"
      contains: "PickupLabel"
  key_links:
    - from: "inventory_ui"
      to: "player.increase_health"
      via: "E key press -> _use_selected() -> _player.increase_health()"
      pattern: "increase_health"
    - from: "inventory_ui"
      to: "world scene tree"
      via: "Q key press -> _drop_selected() -> get_tree().current_scene.add_child()"
      pattern: "add_child"
    - from: "player.item_collected"
      to: "world.pickup_label"
      via: "signal connection in world._ready()"
      pattern: "_on_item_collected"
---

<objective>
Human smoke test to verify all three Phase 3 requirements work end-to-end in the running game.

Purpose: Automated tests cover pure logic; this checkpoint confirms the full interactive flow works as the player experiences it.
Output: Confirmed working Phase 3 features. Phase 3 complete.
</objective>

<execution_context>
@/Users/nicce/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicce/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-item-management/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated gate before human verification</name>
  <files></files>
  <action>
Run the full automated test suite and confirm all checks pass before requesting human verification:

```
make lint && make format-check && make test
```

All 8 item management tests must be GREEN. No lint or format errors. This confirms the implementation is correct at the unit level before the smoke test.
  </action>
  <verify>
    <automated>cd /Users/nicce/projects/github.com/nicce/open-world && make lint && make format-check && make test</automated>
  </verify>
  <done>make lint, make format-check, and make test all pass with zero failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test — verify ITEM-01, ITEM-02, ITEM-03 in running game</name>
  <action>Human verifies the complete Phase 3 interactive flow in Godot.</action>
  <what-built>
Complete Phase 3 implementation:
- Slot selection (click to select, click again to deselect, close panel clears selection)
- E key: uses selected consumable (HealthItem), restores HP, decrements slot quantity
- Q key: drops 1 unit of selected item as a collectable spawned near player position
- Pickup notification: brief "+ [Item Name]" label at bottom-center when any item is collected
  </what-built>
  <how-to-verify>
Open Godot and run the game (F5). Perform the following smoke test:

**1. Slot selection:**
- Press Tab or I to open inventory
- Click on an occupied slot — it should show a highlighted border
- Click the same slot again — highlight should disappear (deselect)
- Click an empty slot — nothing should happen
- Close the inventory (Tab/I) with a slot selected — reopen and verify no slot is highlighted

**2. Use item (ITEM-01):**
- Ensure player has a Medpack/health item in inventory
- Take some damage from an enemy to reduce HP below max
- Open inventory, select the Medpack slot, press E
- Verify: HP increases (health bar), Medpack quantity decrements by 1 (or slot clears if last unit)
- Open inventory, select a Gold Key slot, press E — nothing should happen (silent no-op)

**3. Drop item (ITEM-02):**
- Open inventory, select a Gold Key slot, press Q
- Close inventory
- Verify: a Gold Key collectable appears in the world near the player
- Walk over to it and press E — it should be collectible again

**4. Pickup notification (ITEM-03):**
- Walk over any collectable item and pick it up (press E)
- Verify: a brief "+ [Item Name]" label appears at the bottom-center of the screen
- Verify: the label fades out after ~2 seconds
- Pick up two items in quick succession — verify the label text updates to the most recent item
  </how-to-verify>
  <verify>manual — see how-to-verify above</verify>
  <done>All 4 smoke test scenarios pass as described.</done>
  <resume-signal>Type "approved" if all 4 scenarios work correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human confirmation that all ITEM-01, ITEM-02, and ITEM-03 work interactively in the running game.
</verification>

<success_criteria>
- make lint + make format-check + make test all pass
- Human verifies: slot selection highlights correctly, E uses consumable (HP restored, quantity drops), Q drops item (collectable spawns in world), pickup notification shows and fades
- Phase 3 requirements ITEM-01, ITEM-02, ITEM-03 all confirmed complete
</success_criteria>

<output>
After completion, create `.planning/phases/03-item-management/03-04-SUMMARY.md`
</output>
